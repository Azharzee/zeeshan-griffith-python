# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git & GitHub Workflow

- **Always** create a GitHub repo for every new project, push to `https://github.com/Azharzee/`, and use `main` as the default branch.
- Commit after meaningful units of work with clean, descriptive messages.
- Push to GitHub after every commit so there is always a remote backup.
- GitHub account: **Azharzee** — git identity already configured globally.

## Project Context

This repository holds Python coursework/projects for Zeeshan (Griffith University). Each sub-project or assignment should be in its own subdirectory.

## Python Conventions

- Use Python 3. Run scripts with `python <script.py>`.
- No build step required — files run directly.
- If a project grows to need dependencies, use a `venv` and a `requirements.txt`.
